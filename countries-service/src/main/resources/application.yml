server:
  port: ${PORT:8080}
  servlet:
    context-path: ${BASE_URL:/}

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: user_service_db